import 'package:flutter/material.dart';

class MIRPage extends StatelessWidget {
  const MIRPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MIR'));
  }
}
