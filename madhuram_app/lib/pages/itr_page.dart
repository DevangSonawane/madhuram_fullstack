import 'package:flutter/material.dart';

class ITRPage extends StatelessWidget {
  const ITRPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ITR'));
  }
}
